# AIM v4.0.0 Release-Checks

Stand: 2026-06-18

Diese Datei ist die finale Kontrollliste fuer den AIM-v4.0.0-Release. Ziel ist, vor einem erneuten VSIX-/GitHub-/npm- oder Marketplace-Schritt sicherzustellen, dass keine alten 3.8.x-/3.9.x-Reste, Schema-Brueche oder CI-Fallen uebrig sind.

## 1. Git- und Arbeitsstand

- [x] `git status` ist bewusst sauber oder enthaelt nur gewollte Release-Dateien.
  - Stand 2026-06-18: Nur `.ai-v4.0.0-checks.md` ist untracked.
- [x] `.ai.json-conzept.md` ist bewusst committed oder bewusst untracked gelassen.
  - Stand 2026-06-18: Entscheidung geaendert: Datei bleibt lokal privat, wurde aus dem Git-Index entfernt und in `.gitignore` aufgenommen.
- [x] Letzter Commit enthaelt mindestens:
  - `.ai.json`
  - `CHANGELOG.md`
  - `validate.js`
  - `package.json`
  - `package-lock.json`
- [x] GitHub Actions fuer `validate.yml` sind gruen.
  - Stand 2026-06-18: GitHub Actions Screenshot zeigt alle vier Jobs gruen.
- [x] Kein versehentliches Secret, Token, Passwort oder Private-Key-Material wurde committed.
  - Stand 2026-06-18: Rudimentaerer Pattern-Check auf Release-Dateien ohne Treffer.

## 2. Versionen

- [x] `package.json` steht auf `4.0.0`.
- [x] `package-lock.json` Root-Version steht auf `4.0.0`.
- [x] `package-lock.json` Packages-Root `packages[""].version` steht auf `4.0.0`.
- [x] `.ai.json` `_meta.version` steht auf `4.0.0`.
- [x] `.ai.json` `project.version` steht auf `4.0.0`.
- [x] `.ai.json.example` `project.version` steht auf `4.0.0`.
- [x] `CHANGELOG.md` hat `## [4.0.0] - 2026-06-17` als aktuellen Release-Block.
- [x] `vscode-extension/package.json` steht auf `4.0.0`, falls die VS Code Extension neu gebaut wird.
- [x] `vscode-extension/package-lock.json` steht auf `4.0.0`, falls vorhanden und relevant.
- [x] Der gebaute VSIX-Dateiname enthaelt `4.0.0`.
  - Stand 2026-06-18: `vscode-extension/corex-ai-mindlayer-4.0.0.vsix` wurde lokal neu gebaut; VSIX-Dateien bleiben durch `.gitignore` aus dem Repo.

## 3. Schema und Validator

- [x] `validate.js` registriert AJV-Schemas mit stabilen lokalen Keys, nicht mehr implizit ueber leere `$id`.
  - Stand 2026-06-18: `ajv.addSchema(schema, schemaKey)` und `ajv.getSchema(schemaKey)` werden genutzt.
- [x] `npm run validate:example` ist gruen.
- [x] `npm run validate:all` ist gruen.
- [x] `.ai.json.example` validiert gegen `vscode-extension/src/schemas/ai.schema.json`.
- [x] `.ai.json` validiert gegen `vscode-extension/src/schemas/ai.schema.json`.
- [x] Feature-Index-Schema liegt unter `vscode-extension/src/schemas/ai.features.index.schema.json`.
- [x] Feature-Details-Schema liegt unter `vscode-extension/src/schemas/ai.features.details.schema.json`.
- [x] Keine Schema-Datei braucht zwingend eine eigene `$id`, solange `validate.js` lokale Keys nutzt.

## 4. Lokale Checks

Empfohlene Befehle:

```powershell
cd D:\xampp\htdocs\corex-ai-mindlayer
npm run validate:example
npm run validate:all
npm run lint
php -l ai-init.php
```

Erwartung:

- [x] Alle Befehle laufen ohne Fehler durch.
  - Stand 2026-06-18: `npm run validate:example`, `npm run validate:all`, `npm run lint` und `php -l ai-init.php` sind gruen.
- [x] `npm run validate:example` zeigt `@aiqia/corex-ai-mindlayer@4.0.0`.
- [x] `npm run validate:all` validiert `.ai.json.example` und `.ai.json`.
- [x] `npm run lint` validiert JSON-Syntax und PHP-Syntax.

## 5. GitHub Actions

Workflow: `.github/workflows/validate.yml`

- [x] Job `Validate .ai.json Schema` ist gruen.
  - Stand 2026-06-18: Nach Push vom User bestaetigt.
- [x] Job `Lint PHP Files` ist gruen.
  - Stand 2026-06-18: Nach Push vom User bestaetigt.
- [x] Job `Test AI Integration` ist gruen.
  - Stand 2026-06-18: Nach Push vom User bestaetigt.
- [x] Job `Test With Composer Installation` ist gruen.
  - Stand 2026-06-18: Nach Push vom User bestaetigt.
- [x] Der Schritt `Validate against schema` laeuft ohne AJV-Doppel-ID-Fehler.
  - Stand 2026-06-18: Lokal mit `npm run validate:example` und `npm run validate:all` bestaetigt; CI final nach Push pruefen.
- [x] Der Schritt `Test example validation` laeuft ohne AJV-Doppel-ID-Fehler.
  - Stand 2026-06-18: Lokal mit `npm run validate:example` bestaetigt; CI final nach Push pruefen.
- [x] CI-Ausgabe zeigt `@aiqia/corex-ai-mindlayer@4.0.0`, nicht `3.8.0`.
  - Stand 2026-06-18: Lokale NPM-Scriptausgabe zeigt `@aiqia/corex-ai-mindlayer@4.0.0`; CI final nach Push pruefen.

## 6. VSIX / VS Code Extension

Vor neuem VSIX-Build pruefen:

- [x] `vscode-extension/package.json` Version ist `4.0.0`.
- [x] Extension-README beschreibt v4 korrekt.
- [x] Commands sind noch korrekt registriert.
  - Stand 2026-06-18: `initialize`, `validateWorkspace`, `copyContext`, `installAnchors`, `prepareOnboarding` und `addDetails` sind in `package.json` und `extension.ts` registriert.
- [x] Snippets fuer `ai-root`, `ai-index` und `ai-details` passen zum v4-Schema.
  - Stand 2026-06-18: Snippets sind jetzt auch im Extension-Manifest registriert.
- [x] Templates unter `vscode-extension/src/templates/` passen zum v4 Root Compass.
  - Stand 2026-06-18: Runtime-Templates werden per Compile-Schritt nach `out/templates/` kopiert.
- [x] Extension-Schemas unter `vscode-extension/src/schemas/` sind aktuell.
  - Stand 2026-06-18: Runtime-Schemas werden per Compile-Schritt nach `out/schemas/` kopiert.
- [x] Extension wurde neu kompiliert, falls TypeScript geaendert wurde.
  - Stand 2026-06-18: `npm run compile` ist gruen.
- [x] Alter VSIX-Build wird nicht versehentlich erneut hochgeladen.
  - Stand 2026-06-18: Alte VSIX-Dateien wurden aus Git entfernt; `*.vsix` ist ignoriert.
- [x] Neuer VSIX-Dateiname enthaelt `4.0.0`.
  - Stand 2026-06-18: `corex-ai-mindlayer-4.0.0.vsix` wurde lokal erfolgreich gebaut.

Moegliche Befehle je nach eingerichtetem Script:

```powershell
cd D:\xampp\htdocs\corex-ai-mindlayer\vscode-extension
npm install
npm run compile
npm run package
```

Falls kein `package`-Script existiert, VSIX mit dem lokal verwendeten Packaging-Tool bauen und den Dateinamen/Manifeststand manuell pruefen.

## 7. Dokumentation

- [x] `README.md` enthaelt keine prominenten alten Aussagen wie "v3.8.5" als aktueller Stand.
  - Stand 2026-06-18: README wurde als schlanker v4.0.0-Einstieg neu gefasst.
- [x] `README.md` erklaert AIM v4 als "Compass, not Chronicle".
- [x] `CONTRIBUTING.md` passt zum v4-Workflow.
  - Stand 2026-06-18: Regeln betonen schlanken Compass, Schema-Validierung und keine Auto-Writes ohne User Consent.
- [x] `CHANGELOG.md` dokumentiert den v4-Reset, AJV-Fix, `.ai.json`-Migration und Version-Sync.
- [x] `.ai.json-conzept.md` ist bewusst privat gehalten.
  - Stand 2026-06-18: Datei ist nicht mehr im README verlinkt, wurde aus dem Git-Index entfernt und in `.gitignore` aufgenommen.
- [x] Dokumentation verweist nicht auf geloeschte Pflichtdateien wie `INITIALIZE.md`, sofern diese nicht wiederhergestellt werden sollen.
  - Stand 2026-06-18: Nur ein historischer Changelog-Eintrag zu `INITIALIZE.md.new` bleibt erhalten.

## 8. Repository-Hygiene

- [x] Keine alten Build-Artefakte unbeabsichtigt committed.
  - Stand 2026-06-18: Getrackte `vscode-extension/out/*`-Dateien und VSIX-Artefakte wurden aus dem Repo entfernt.
- [x] `node_modules/` ist nicht committed.
- [x] `vendor/` ist nicht committed, sofern nicht bewusst noetig.
- [x] Alte `.new`, `.tmp`, `.bak`, `.orig` Dateien sind nicht committed.
  - Stand 2026-06-18: Solche Dateien existieren lokal/ignoriert, aber nicht getrackt.
- [x] Lokale Backups bleiben ignoriert.
  - Stand 2026-06-18: Getrackte Backup-Archive wurden aus dem Repo entfernt; neue Backups bleiben durch `.gitignore` ignoriert.
- [x] `.gitignore` passt zum finalen Repo-Ziel.
  - Stand 2026-06-18: `.gitignore` ignoriert Dependencies, lokale Konfiguration, Logs, Temp-Dateien, Backup-Pfade, `vscode-extension/out/` und `*.vsix`.

## 9. Offene bekannte Nachkontrollen

- [x] Entscheiden, ob `.ai.json-conzept.md` dauerhaft im Repo bleiben soll.
  - Stand 2026-06-18: Entscheidung getroffen: nein, bleibt lokal privat und wird ignoriert.
- [x] README-Versionen von 3.x auf 4.0.0 aktualisieren, falls sie noch als aktueller Stand dargestellt werden.
  - Stand 2026-06-18: README und Extension-README beschreiben v4.0.0 bzw. AIM v4 ohne alte aktuelle 3.x-Aussage.
- [x] VSIX neu bauen und Dateiname/Manifest pruefen.
  - Stand 2026-06-18: `npm run package` ist gruen und erzeugt `corex-ai-mindlayer-4.0.0.vsix`.
- [x] Repository-Hygiene entscheiden: getrackte Backup-Archive und alte Build-/VSIX-Artefakte entfernen oder bewusst als historisches Archiv behalten.
  - Stand 2026-06-18: Entscheidung getroffen: radikal raus. Backups, `out/` und VSIX-Artefakte wurden aus dem Git-Stand entfernt.
- [x] Optional `npm audit` bewusst bewerten, aber nicht blind mit Breaking Changes fixen.
  - Stand 2026-06-18: `lodash` war ungenutzt und wurde entfernt; `npm audit` meldet im Root `0 vulnerabilities`.
- [x] Optional `tools/validate-docs.js` an v4-Doku-Realitaet anpassen, falls `npm test` wieder als Release-Gate dienen soll.
  - Stand 2026-06-18: `tools/validate-docs.js` prueft jetzt v4-Doku, private Konzeptdatei, Versionskonsistenz und alte Pflichtverweise; `npm test` ist gruen.

## 10. Release-Freigabe

Freigabe erst setzen, wenn:

- [x] GitHub Actions gruen sind.
- [x] Lokale Validierung gruen ist.
- [x] VSIX neu gebaut und versionskorrekt ist.
- [x] README/CHANGELOG nicht mehr widerspruechlich wirken.
- [x] Keine alten 3.8.x-/3.9.x-Hinweise als aktueller Stand uebrig sind.
